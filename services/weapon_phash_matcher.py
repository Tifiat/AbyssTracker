"""
Legacy pHash weapon matcher entry point.

The old implementation was moved to services.weapon_phash_matcher_legacy.
Do not import it from runtime code. Keep the legacy file only as reference
material while the DINOv2 matcher is being rebuilt.
"""
