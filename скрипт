🎮 Проект: AbyssTracker

Идея: десктопное приложение для сохранения и визуального сравнения забегов Витой Бездны в Genshin Impact.

Стек: Python + PySide6

Фокус: UI > логика, визуальная компактность, минимум текста, аккуратный интерфейс.

Структура проекта:

AbyssTracker/
│
├─ main.py
├─ README.txt
├─ state.json
├─ runs_history.json
│
├─ assets/
│  ├─ characters/
│  └─ weapons/
│
├─ debug/
│
├─ models/
│  └─ character.py
│
├─ parser/
│  ├─ __init__.py
│  └─ hoyolab_parser.py
│
└─ ui/
   ├─ __init__.py
   ├─ main_window.py
   ├─ run_history_window.py
   ├─ run_history_window_bkp.py
   └─ widgets/
      ├─ drag.py
      ├─ drop.py
      ├─ team.py
      ├─ team_row.py       # текущая версия с QTimer и set_scale
      ├─ run_card.py       # текущая версия с масштабированием
      ├─ run_history_container.py
      ├─ flow_layout.py
      ├─ timers.py
      └─ __init__.py


Ключевые моменты логики:

TeamSlot: персонаж + оружие + артефакт. Оружие — правый нижний угол, артефакт — левый нижний угол.

TeamRow: массивы char_icons, weapon_icons, artifact_icons, floor_labels. Масштабирование через set_scale(). Позиции оружия и артефактов теперь корректно обновляются при первом открытии и повторном открытии окна истории.

RunCard: хранит команды, финальный таймер, масштабируется через set_scale().

RunHistoryWindow: FlowLayout, вертикальные линии через RunHistoryContainer, масштабирование Ctrl+Wheel (1.0–2.5×), прокрутка работает отдельно.

FlowLayout: кастомный, без создания лишних виджетов.

Баги, которые исправлены:

Масштабирование Ctrl+Wheel корректно изменяет размер персонажей, оружия, артефактов и чисел.

Повторное открытие окна истории не сбрасывает позиции оружия и артефактов.

Вертикальные разделители всегда смещаются правильно при масштабировании.

Первое отображение карточек корректное, числа и таймеры не искажаются.

RunCard и TeamRow совместимы с текущей структурой данных.

Текущее состояние: полностью рабочее, баги с позиционированием исправлены, масштабирование и vertical lines работают идеально.